# Хичээлийн шаардлагын нийцлийн баталгаа (Course Compliance)

**Төсөл:** EduTest — Оролт/Гаралтын тестийн систем (MVP)
**Хавтас:** `edutest-python/`
**Шалгасан огноо:** 2026-09-01
**Тестийн үр дүн:** 141/141 амжилттай (`python3 -m unittest discover -s tests`)

---

## 1. Хэрэглэсэн технологи

| Шаардлага | Байдал | Нотолгоо |
| --- | --- | --- |
| Үндсэн хэл нь **Python** | ✅ | Бүх бизнес логик `app.py`, `database.py`, `domain.py`, `seed.py` дотор. Гүйцэтгэх код зөвхөн `.py` файлууд. |
| Веб фрэймворк нь **Flask** | ✅ | `app.py` дотор `from flask import ...`. |
| Локал өгөгдлийн сан нь **SQLite** | ✅ | `database.py` дотор Python-ий built-in `sqlite3`. Өгөгдлийн сан нь `edutest.db` нэг файл. |
| Стандарт сангуудыг эрхэмлэсэн | ✅ | `sqlite3`, `csv`, `hashlib`, `hmac`, `secrets`, `re`, `unicodedata`, `io`, `os`, `datetime`, `functools`, `pathlib`, `unittest` — бүгд стандарт сан. |

**Гадаад хамаарал: 5 сан (4 нь заавал бус).**

| Сан | Юунд | Заавал юу |
| --- | --- | --- |
| `Flask` | Веб фрэймворк | Тийм — системийн үндэс |
| `openpyxl` | Excel (.xlsx) экспорт | Үгүй — байхгүй бол зөвхөн Excel татах ажиллахгүй |
| `python-docx` | Word (.docx) тайлан | Үгүй — байхгүй бол зөвхөн Word татах ажиллахгүй |
| `qrcode` | QR кодыг SVG болгох | Үгүй — холбоосыг гараар хуулж болно |
| `matplotlib` | Word тайлангийн график | Үгүй — байхгүй бол тайлан графикгүй үүснэ |

Сүүлийн дөрөв нь **зөвхөн экспорт/QR-т** зориулагдсан. Үндсэн ажиллагаа —
нэвтрэлт, тест үүсгэх, оюутны илгээлт, оноо бодох, Pre/Post тааруулалт,
CSV экспорт — бүгд **стандарт сан + Flask**-аар л хийгддэг.

> **Анхаар:** Хэрэв хичээлийн шаардлага «гадаад хамаарал зөвхөн Flask»
> гэсэн бол `openpyxl`, `python-docx`, `qrcode`-ийг хасаж, холбогдох
> маршрутуудыг (`/pairs/<id>/comparison.xlsx`, `/pairs/<id>/report.docx`,
> `/tests/<id>/qr.svg`) идэвхгүй болгох боломжтой. CSV экспорт нь
> стандарт сангийн `csv` модулиар хийгдсэн тул хэвээр ажиллана.

---

## 2. Ашиглаагүй технологиуд (хориглосон жагсаалт)

| Технологи | Ашигласан уу | Тайлбар |
| --- | --- | --- |
| React | ❌ Үгүй | `.jsx`, `.tsx` файл байхгүй. UI нь Jinja2 + энгийн HTML + CSS. Дизайны **харагдах байдлыг** өмнөх React хувилбараас лавлагаа болгосон боловч ямар ч код хуулаагүй. |
| Node.js / npm | ❌ Үгүй | `package.json`, `node_modules`, `.mjs` файл байхгүй. Build алхам байхгүй. |
| Vite / bundler | ❌ Үгүй | Статик файлыг Flask шууд түгээнэ. |
| Supabase | ❌ Үгүй | Клиент сан ч, дуудлага ч байхгүй. |
| Firebase | ❌ Үгүй | — |
| PostgreSQL | ❌ Үгүй | Локал MVP нь зөвхөн SQLite. PostgreSQL нь **зөвхөн ирээдүйн шилжилтийн төлөвлөгөө** (README §5). |
| Docker | ❌ Үгүй | `Dockerfile`, `docker-compose.yml` байхгүй. |
| Гадаад API дуудлага | ❌ Үгүй | `requests`, `urllib.request`, `fetch` байхгүй. Chart.js нь CDN-ээс биш `static/vendor/`-оос локалаар ачаалагдана. Интернэтгүй ажиллана. |
| Машин сургалтын runtime | ❌ Үгүй | `pandas`, `scikit-learn`, `torch`, `tensorflow` ашиглаагүй. Оноог энгийн арифметикаар бодно (`domain.score_attempt`). `numpy` нь matplotlib-ийн дотоод хамаарлаар ордог ч кодод шууд ашиглаагүй. |
| Тусдаа өгөгдлийн сангийн сервер | ❌ Үгүй | SQLite нь файл дээр ажилладаг тул суулгац шаардахгүй. |
| Шинэ IDE / шинэ програм | ❌ Үгүй | Компьютер дээр байгаа Python 3-ийг шууд ашиглана. |

Шалгах тушаал:

```bash
grep -rniE "react|node_modules|supabase|firebase|psycopg|postgres|docker|requests|tensorflow|torch|sklearn" \
  --include="*.py" --include="*.html" --include="*.txt" .

# Түлхүүр, нууц утга шалгах
grep -rniE "api[_-]?key|anon_key|secret.*=.*['\"][a-z0-9]{16}" --include="*.py" .
```

Гарах цорын ганц илэрц нь **тайлбар мөрүүд** — «эдгээрийг ашиглаагүй» эсвэл
«PostgreSQL руу ирээдүйд шилжих хил» гэсэн баримтжуулалт.

---

## 3. Өгөгдөл хаана хадгалагдах вэ

**Бүх оперативын өгөгдөл нь локал SQLite файлд (`edutest.db`) хадгалагдана.**

| Өгөгдөл | Хүснэгт |
| --- | --- |
| Багш/админы бүртгэл, хэшлэгдсэн нууц үг | `users` |
| Хичээл, ангийн групп | `courses`, `class_groups` |
| Тестийн хос, тестүүд, асуултууд | `test_pairs`, `tests`, `questions` |
| Оюутны бүртгэл (зохиомол демо) | `students` |
| Оролдлого, хариулт, оноо | `attempts`, `answers` |

**Хадгалалтын хориглосон хэлбэрүүд — ашиглаагүй:**

* `localStorage` / `sessionStorage` — ❌ (templates дотор JS хадгалалт байхгүй)
* Hardcoded JavaScript өгөгдөл — ❌ (цорын ганц JS нь устгахын өмнөх `confirm()`)
* Гадаад үүлэн сан — ❌
* Түр зуурын файл, кэш — ❌

Cookie-д зөвхөн **Flask session** (нэвтэрсэн багшийн `user_id`) хадгалагдана —
оперативын өгөгдөл биш.

---

## 4. Аюулгүй байдал ба өгөгдлийн нууцлал

* Нууц үг нь **PBKDF2-HMAC-SHA256** (240,000 давталт, 16 байт санамсаргүй давс).
  Ил задгай нууц үг хэзээ ч хадгалагдахгүй — `users.password_hash` бүх мөр
  `pbkdf2_sha256$` угтвартай.
* Багцад **бодит оюутны мэдээлэл байхгүй** — `edutest.db` доторх 6 оюутан,
  3 хэрэглэгч бүгд `seed.py`-ээр үүссэн **зохиомол** бичлэг.
* **API түлхүүр, нууц үг, гадаад өгөгдлийн сангийн холболтын мэдээлэл байхгүй.**
  `.env` файл байхгүй.
* Демо нэвтрэлтийн нууц үг (`admin1234`, `demo1234`) нь **зориудаар нийтэлсэн
  сургалтын жишээ** — README-д баримтжуулсан.
* `app.secret_key` нь тохируулаагүй үед хөгжүүлэлтийн түр утга авна;
  байршуулахдаа `EDUTEST_SECRET` орчны хувьсагчаар солино (README §6).
* Бүх SQL параметрчилсэн (`?`) — SQL injection-оос хамгаалагдсан.
  Jinja2 автомат escape — XSS-ээс хамгаалагдсан.

---

## 5. Дүгнэлт

Энэ төсөл нь хичээлийн бүх заавал шаардлагыг хангасан: **Python** үндсэн хэл,
**Flask** веб фрэймворк, **SQLite** локал өгөгдлийн сан, стандарт сангуудыг
дээд зэргээр ашигласан, хориглосон технологи нэг ч байхгүй, бүх оперативын
өгөгдөл локал SQLite файлд хадгалагдана.

React + Supabase хувилбар нь **тусдаа хавтас** (`Desktop/edutest`) дээр
хэвээр үлдсэн — энэ багцад ороогүй, өөрчлөгдөөгүй.
